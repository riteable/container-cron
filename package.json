{
  "name": "service-cron",
  "version": "1.0.0",
  "description": "A cron job scheduler for Docker containers compatible with Swarm mode.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Riteable",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.7.1",
    "cron": "^1.7.2",
    "dockerode": "^3.0.2"
  },
  "devDependencies": {
    "standard": "^16.0.3"
  }
}
